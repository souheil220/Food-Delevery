import 'package:flutter/material.dart';

class MyLocation extends StatelessWidget {
 // var order;
  //MyLocation(this.order);
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.location_on);
  }
}
